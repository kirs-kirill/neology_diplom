---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: my-app
  name: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - image: kirskirill/diplomapp:latest
          imagePullPolicy: IfNotPresent
          name: my-app
          
---
apiVersion: v1
kind: Service
metadata:
  name: my-app
spec:
  ports:
    - name: web
      port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: my-app
  type: NodePort